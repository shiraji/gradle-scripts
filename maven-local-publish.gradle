apply plugin: 'maven-publish'

publishing {
    publications {
        maven(MavenPublication) {
            groupId GROUPID
            artifactId ARTIFACTID
            version "${VERSION}-SNAPSHOT"
        }
    }
    repositories {
        maven {
            mavenLocal()
        }
    }
}
